import Placeholder from "./Placeholder";
const ScanHistory = () => (
  <Placeholder
    title="Scan history"
    description="Chronological list of all past scans with date, Vitality score, zone, delta, and capture-quality summary."
  />
);
export default ScanHistory;
